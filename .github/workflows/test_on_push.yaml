name: Run tests on any Push event
# Запуск при любом push евенте в любой ветке, за исключением релизных тэгов.
# Они будт тестироваться перед сборкой
on:
  push:
    tags:
      - '!refs/tags/*'
    branches:
      - '*'
jobs:
  run_tests:
    runs-on: [self-hosted]
    steps:
      - name: python ver
        run: python --version
      - name: pwd
        run: pwd
      - name: Run linters
        run: black ./mlops/features.py

